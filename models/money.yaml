title: Money
type: object
properties:
  amount:
    type: integer
    minimum: 0
  currency:
    type: string
    example: EUR
    minLength: 3
    maxLength: 3
    pattern: '[A-Z]{3}'
required:
  - amount
  - currency
description: ''
